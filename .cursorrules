- Always use Tailwind CSS for styling.
- Use TypeScript with strict types.
- Follow the existing project structure from Lovable.
- Keep the app frontend-only unless a backend is explicitly requested.
- If a submission fails, log the full error to the console for debugging.